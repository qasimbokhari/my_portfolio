{
  "name": "qasim-reviews-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:create": "wrangler d1 create qasim-reviews",
    "d1:migrate": "wrangler d1 execute qasim-reviews --local --file=./migrations/0001_initial.sql"
  },
  "devDependencies": {
    "wrangler": "^4.107.0"
  }
}
